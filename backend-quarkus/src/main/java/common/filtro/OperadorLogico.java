package common.filtro;

// Combinador lógico aplicado a todos os critérios de um filtro (estrutura plana, sem aninhamento).
// Quando ausente, o padrão é AND.
public enum OperadorLogico {

    AND,
    OR

}
